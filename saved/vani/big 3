#include<stdio.h>
int main()
{
int a,b,c,n;
printf("entet the no.");
scanf("%d%d%d",&a,&b,&c);
       if(a>b&&a>c)
			n=a;
	if(b>a&&b>c)
			n=b;
	if(c>a&&c>b)
			n=c;
printf("the big=%d",n);
return 0;
}
