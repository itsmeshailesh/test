#include<stdio.h>

float fact(float);
int power(int);

int main()
{
float a;
float sum=0
printf("Enter the no.:");
scanf("%d",&a);
while(i<=a>
	sum=i*i/fact(i)
printf("answer is: %d",);
return 0;
}
float fact(float a)
{
int f=1;
for(int i=1;i<=a;i++)
	f*=i;
return f;
}
int power(int a,int b)
{
	int p=1;
	for(int i=
