#include<stdio.h>

int fact(int);

int main()
{
int a;
printf("Enter the no.:");
scanf("%d",&a);
while(i<=
printf("answer is: %d",);
return 0;
}
int fact(int a)
{
int f=1;
for(int i=1;i<=a;i++)
	f*=i;
return f;
}
